System.register([], function(exports_1, context_1) {
    "use strict";
    var __moduleName = context_1 && context_1.id;
    return {
        setters:[],
        execute: function() {
            describe('Netattraction', function () {
                it('displayed in descending netattraction order from left to right', function () {
                    expect(1).toEqual(2);
                });
                it('% best for each concept should be displayed above the x axis', function () {
                    expect(1).toEqual(2);
                });
                it('% worst for each concept should be displayed below the x axis', function () {
                    expect(1).toEqual(2);
                });
                it('best % and worst % should be stacked so there is 100% overlap on the bars', function () {
                    expect(1).toEqual(2);
                });
            });
        }
    }
});
//# sourceMappingURL=netattraction.spec.js.map
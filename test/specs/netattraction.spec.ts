

import { Netattraction } from '../../src/charts/netattraction';

describe('Netattraction', () => {
	it('displayed in descending netattraction order from left to right', () => {
		expect(1).toEqual(2);
	});

	it('% best for each concept should be displayed above the x axis', () => {
		expect(1).toEqual(2);
	});

	it('% worst for each concept should be displayed below the x axis', () => {
		expect(1).toEqual(2);
	});

	it('best % and worst % should be stacked so there is 100% overlap on the bars', () => {
		expect(1).toEqual(2);
	});
});
